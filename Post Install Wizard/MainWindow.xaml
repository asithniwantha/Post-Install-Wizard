<Window x:Class="Post_Install_Wizard.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Post_Install_Wizard"
        xmlns:viewmodels="clr-namespace:Post_Install_Wizard.ViewModels"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Windows Post Install"
        WindowState="Maximized"
        WindowStyle="None"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Window.DataContext>
        <viewmodels:MainViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left Sidebar -->
        <Border Grid.Column="0" Background="{DynamicResource MaterialDesignDarkBackground}" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Top Logo / Title area if needed -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,32">
                    <materialDesign:PackIcon Kind="MicrosoftWindows" Width="32" Height="32" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    <TextBlock Text="Windows" FontSize="24" FontWeight="Bold" Margin="8,0,0,0" VerticalAlignment="Center" />
                </StackPanel>

                <!-- Navigation Buttons -->
                <StackPanel Grid.Row="1">
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding BeginInstallCommand}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,4">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="PlayCircle" Width="24" Height="24" Foreground="LightGreen" />
                            <TextBlock Text="Begin Install" Margin="16,0,0,0" FontSize="16" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding SelectDefaultsCommand}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,4">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CheckboxMarkedCircle" Width="24" Height="24" Foreground="SkyBlue" />
                            <TextBlock Text="Select Defaults" Margin="16,0,0,0" FontSize="16" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding SelectAllCommand}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,4">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CheckAll" Width="24" Height="24" Foreground="SkyBlue" />
                            <TextBlock Text="Select All" Margin="16,0,0,0" FontSize="16" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding SelectNoneCommand}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,4">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CheckboxBlankCircleOutline" Width="24" Height="24" Foreground="SkyBlue" />
                            <TextBlock Text="Select None" Margin="16,0,0,0" FontSize="16" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>

                    <Separator Margin="0,16" Background="#555" />

                    <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding OptionsCommand}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,4">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Cog" Width="24" Height="24" Foreground="LightGray" />
                            <TextBlock Text="Options" Margin="16,0,0,0" FontSize="16" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding ConfigCommand}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,4">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Wrench" Width="24" Height="24" Foreground="Yellow" />
                            <TextBlock Text="Config" Margin="16,0,0,0" FontSize="16" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding ManualCommand}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,4">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="BookOpen" Width="24" Height="24" Foreground="White" />
                            <TextBlock Text="Manual" Margin="16,0,0,0" FontSize="16" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding AboutWPICommand}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,4">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="HelpCircle" Width="24" Height="24" Foreground="Orange" />
                            <TextBlock Text="About WPI" Margin="16,0,0,0" FontSize="16" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- Timer -->
                <StackPanel Grid.Row="2" HorizontalAlignment="Center">
                    <TextBlock Text="The installer&#x0a;will start in" TextAlignment="Center" FontSize="14" Foreground="#CCC"/>
                    <TextBlock Text="{Binding TimeLeftText}" FontSize="36" FontWeight="Bold" TextAlignment="Center" Margin="0,8" />
                    <ProgressBar Minimum="0" Maximum="100" Value="{Binding ProgressValue}" Height="10" Width="150" Foreground="LightGreen" />
                    <TextBlock Text="Click to stop the timer" TextAlignment="Center" FontSize="10" Foreground="#AAA" Margin="0,4,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Window Controls -->
        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="16" Panel.ZIndex="1">
            <Button Style="{StaticResource MaterialDesignIconForegroundButton}" ToolTip="Minimize" Click="MinimizeButton_Click" Width="36" Height="36">
                <materialDesign:PackIcon Kind="WindowMinimize" Width="24" Height="24" />
            </Button>
            <Button Style="{StaticResource MaterialDesignIconForegroundButton}" ToolTip="Close" Click="CloseButton_Click" Margin="8,0,0,0" Foreground="#ef5350" Width="36" Height="36">
                <materialDesign:PackIcon Kind="Close" Width="24" Height="24" />
            </Button>
        </StackPanel>

        <!-- Main Content Area -->
        <Border Grid.Column="1" Padding="32">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="General" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,16" />
                <Separator Grid.Row="0" VerticalAlignment="Bottom" Margin="0,0,0,8" Background="#555" />

                <!-- Checkbox List -->
                <ItemsControl Grid.Row="1" ItemsSource="{Binding GeneralSoftware}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected}" Margin="0,4" FontSize="16" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <!-- Display items in a wrap panel to mimic columns if more items are added -->
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Vertical" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </Grid>
        </Border>
    </Grid>
</Window>